#  dyshp:  peter's frontend for dysh

By default, a new fresh anacona3 will be installed. 


1. `install1`:  install using `uv`, but use the `.venv` , not `uv run`
2. `install2`:  install using `pip`
